import { Component, ViewEncapsulation } from '@angular/core';

/** Рассылка e-mail по событиям live — поля и галочки читает boot.js / live.js. */
@Component({
  selector: 'app-finresp-notify-panel',
  templateUrl: './finresp-notify-panel.component.html',
  encapsulation: ViewEncapsulation.None,
})
export class FinrespNotifyPanelComponent {}
